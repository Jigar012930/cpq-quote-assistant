# cpq-quote-assistant
Retrieves relevant products, pricing rules, and discount policies from the toy catalog (RAG). Calls MCP tools to compute price, apply discounts, validate config. Returns a structured quote with line items, totals, and citations to the source rules
